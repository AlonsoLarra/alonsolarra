'use strict';

/**
 * @ngdoc function
 * @name frontpocketApp.controller:EticaCtrl
 * @description
 * # EticaCtrl
 * Controller of the frontpocketApp
 */
angular.module('frontpocketApp')
  .controller('EticaCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
