'use strict';

/**
 * @ngdoc function
 * @name frontpocketApp.controller:OngsCtrl
 * @description
 * # OngsCtrl
 * Controller of the frontpocketApp
 */
angular.module('frontpocketApp')
  .controller('OngsCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
